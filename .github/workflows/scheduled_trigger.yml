name: Scheduled trigger

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate report
        run: echo "Report generated at $(date -u)" >> report.log